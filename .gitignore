# secrets
.env
*/.env
**/.env
!**/.env.example

# node
node_modules/
npm-debug.log*

# generated data / tokens
*.db
*.db-wal
*.db-shm
investments.db
tokens.json
*.token
schwab-token*.json

# trend logs (optional local artifacts)
*.jsonl
weekly-trend.csv

# os / editor
.DS_Store
*.swp

# python
schwab/.venv/
__pycache__/
*.pyc

# Tax returns: contain SSNs and account numbers — NEVER commit (query locally only)
tax-returns/

# Personal documents (statements, policies, registrations) — local only, never commit
documents/
